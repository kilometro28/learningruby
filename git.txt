git init
git add .
git commit -m "comment"
git remote add origin https://github.com/kilometro28/learningruby.git
git push -u origin master

if an error is shown:

git pull
git fetch
git merge

git status -> to see the status
